# [F1.E03] — Redis: Conexão, Cache Helpers, e Health Check

> **Fase:** 1 — Fundação
> **Épico:** F1.E03
> **Prioridade:** 3 de 7 (nesta fase)
> **Depende de:** Bootstrap (TASK 08 — Docker Compose com Redis)
> **Estimativa de tasks:** 4 tasks

> **⚠️ ANTES DE EXECUTAR:** Leia WBC_REGRAS_INVIOLAVEIS.md. As seguintes regras são absolutas:
> - NUNCA pare, NUNCA pergunte, NUNCA espere confirmação
> - Execução sequencial — sem subagents
> - Literalidade absoluta — se não está no prompt, não existe
> - domain/ NUNCA importa de adapters/
> - tenantId OBRIGATÓRIO em toda query Prisma
> - ZERO strings hardcoded na UI — tudo via i18n
> - ZERO `any` — tipagem estrita
> - Commits: Conventional Commits em inglês
> - Sem testes durante construção
> - Ao concluir este épico → merge → próximo épico → NÃO PARAR

---

## CONTEXTO

Criar conexão Redis singleton, helpers de cache tipados, e health check endpoint. Redis é usado para cache de dados frequentes e como backend do BullMQ.

## PRÉ-CONDIÇÕES

- [x] Docker Compose com Redis configurado
- [x] apps/worker já tem conexão Redis básica

## RESULTADO ESPERADO

- Conexão Redis funcional com reconnect automático
- Helpers de cache tipados (cacheGet, cacheSet, cacheDelete, cacheInvalidatePattern)
- Prefixo `wbc:` em todas as chaves
- Health check endpoint `/api/health/redis`

---

## TASKS

### TASK 1 de 4 — Criar módulo de conexão Redis singleton para API

**Objetivo:** Conexão Redis reutilizável para apps/api.

**Arquivos criados:**
- `apps/api/src/lib/redis.ts` (novo)

**Código:**

```typescript
// apps/api/src/lib/redis.ts
import Redis from 'ioredis';

const REDIS_URL = process.env.REDIS_URL ?? 'redis://localhost:6379/0';

let redis: Redis | undefined;

export function getRedis(): Redis {
  if (!redis) {
    redis = new Redis(REDIS_URL, {
      maxRetriesPerRequest: 3,
      retryStrategy(times) {
        const delay = Math.min(times * 200, 2000);
        return delay;
      },
    });
  }
  return redis;
}
```

**Commit:**
```bash
git add apps/api/src/lib/redis.ts
git commit -m "feat(cache): add Redis singleton connection for API"
```

**PROSSIGA IMEDIATAMENTE para TASK 2.**

---

### TASK 2 de 4 — Criar helpers de cache tipados

**Objetivo:** Helpers genéricos para operações de cache com prefixo automático.

**Arquivos criados:**
- `apps/api/src/lib/cache.ts` (novo)

**Código:**

```typescript
// apps/api/src/lib/cache.ts
import { getRedis } from './redis';

const PREFIX = 'wbc:';
const DEFAULT_TTL = 300; // 5 minutes

function prefixKey(key: string): string {
  return `${PREFIX}${key}`;
}

export async function cacheGet<T>(key: string): Promise<T | null> {
  const redis = getRedis();
  const data = await redis.get(prefixKey(key));
  if (!data) return null;
  return JSON.parse(data) as T;
}

export async function cacheSet<T>(key: string, value: T, ttl: number = DEFAULT_TTL): Promise<void> {
  const redis = getRedis();
  await redis.set(prefixKey(key), JSON.stringify(value), 'EX', ttl);
}

export async function cacheDelete(key: string): Promise<void> {
  const redis = getRedis();
  await redis.del(prefixKey(key));
}

export async function cacheInvalidatePattern(pattern: string): Promise<void> {
  const redis = getRedis();
  const keys = await redis.keys(prefixKey(pattern));
  if (keys.length > 0) {
    await redis.del(...keys);
  }
}

// Cache TTL constants
export const CACHE_TTL = {
  SHORT: 60,        // 1 minute
  MEDIUM: 300,      // 5 minutes
  LONG: 900,        // 15 minutes
  ENTITLEMENTS: 300, // 5 minutes — for plan/feature cache
} as const;
```

**Commit:**
```bash
git add apps/api/src/lib/cache.ts
git commit -m "feat(cache): add typed cache helpers with wbc: prefix and TTL constants"
```

**PROSSIGA IMEDIATAMENTE para TASK 3.**

---

### TASK 3 de 4 — Instalar ioredis na API

**Objetivo:** Adicionar ioredis como dependência do apps/api.

**Ações:**

```bash
cd apps/api
pnpm add ioredis
cd ../..
```

**Commit:**
```bash
git add apps/api/package.json pnpm-lock.yaml
git commit -m "chore(cache): add ioredis dependency to API"
```

**PROSSIGA IMEDIATAMENTE para TASK 4.**

---

### TASK 4 de 4 — Criar health check endpoint

**Objetivo:** Endpoint que verifica se Redis está respondendo.

**Arquivos criados:**
- `apps/api/src/routers/health.ts` (novo)

**Arquivos modificados:**
- `apps/api/src/trpc/router.ts` (adicionar health router)

**Código:**

```typescript
// apps/api/src/routers/health.ts
import { router, publicProcedure } from '../trpc/trpc';
import { getRedis } from '../lib/redis';
import { prisma } from '@wbc/db';

export const healthRouter = router({
  redis: publicProcedure.query(async () => {
    try {
      const redis = getRedis();
      const pong = await redis.ping();
      return { status: 'ok', response: pong };
    } catch (error) {
      return { status: 'error', response: String(error) };
    }
  }),

  db: publicProcedure.query(async () => {
    try {
      await prisma.$queryRaw`SELECT 1`;
      return { status: 'ok' };
    } catch (error) {
      return { status: 'error', response: String(error) };
    }
  }),
});
```

Modificar `apps/api/src/trpc/router.ts`:

```typescript
// apps/api/src/trpc/router.ts
import { router } from './trpc';
import { authRouter } from '../routers/auth';
import { healthRouter } from '../routers/health';

export const appRouter = router({
  auth: authRouter,
  health: healthRouter,
});

export type AppRouter = typeof appRouter;
```

**Commit:**
```bash
git add apps/api/src/routers/health.ts apps/api/src/trpc/router.ts
git commit -m "feat(cache): add health check endpoints for Redis and database"
```

---

## FINALIZAÇÃO DO ÉPICO

1. Atualizar prompts/STATE.json (epic = COMPLETED, avançar para F1.E04)
2. git add prompts/STATE.json
3. git commit -m "state: F1.E03 complete — advancing to F1.E04"
4. git checkout main
5. git merge fase-01/F1.E03_redis_cache --no-ff
6. git branch -d fase-01/F1.E03_redis_cache
7. git checkout -b fase-01/F1.E04_event_bus
8. PROSSEGUIR IMEDIATAMENTE para o próximo épico. NÃO PARAR. NÃO PERGUNTAR.

---

## CRITÉRIOS DE CONCLUSÃO (DoD)

- [ ] Conexão Redis funcional com reconnect automático
- [ ] Helpers de cache tipados funcionando
- [ ] Health check respondendo
- [ ] Prefixo `wbc:` em todas as chaves
