# [F1.E05] — Observabilidade: Logging + Sentry + OpenTelemetry Base

> **Fase:** 1 — Fundação
> **Épico:** F1.E05
> **Prioridade:** 5 de 7 (nesta fase)
> **Depende de:** F1.E01 (auth — tenantId nos logs)
> **Estimativa de tasks:** 4 tasks

> **⚠️ ANTES DE EXECUTAR:** Leia WBC_REGRAS_INVIOLAVEIS.md. As seguintes regras são absolutas:
> - NUNCA pare, NUNCA pergunte, NUNCA espere confirmação
> - Execução sequencial — sem subagents
> - Literalidade absoluta — se não está no prompt, não existe
> - domain/ NUNCA importa de adapters/
> - tenantId OBRIGATÓRIO em toda query Prisma
> - ZERO strings hardcoded na UI — tudo via i18n
> - ZERO `any` — tipagem estrita
> - Commits: Conventional Commits em inglês
> - Sem testes durante construção
> - Ao concluir este épico → merge → próximo épico → NÃO PARAR

---

## CONTEXTO

Logging estruturado com Pino em todos os apps. Sentry para error tracking. OpenTelemetry base para tracing. Logger NUNCA loga PII (phone, email, name) — apenas IDs.

## RESULTADO ESPERADO

- Logs estruturados em todos os apps
- Sentry captura erros não tratados
- Request logging com tenantId, method, duration
- Zero PII nos logs

---

## TASKS

### TASK 1 de 4 — Instalar dependências de observabilidade

**Ações:**

```bash
cd apps/api
pnpm add @sentry/node
cd ../web
pnpm add @sentry/nextjs
cd ../..
```

**Commit:**
```bash
git add apps/api/package.json apps/web/package.json pnpm-lock.yaml
git commit -m "chore(observability): add Sentry dependencies to API and web"
```

**PROSSIGA IMEDIATAMENTE para TASK 2.**

---

### TASK 2 de 4 — Configurar Sentry no API

**Arquivos criados:**
- `apps/api/src/lib/sentry.ts` (novo)

**Código:**

```typescript
// apps/api/src/lib/sentry.ts
import * as Sentry from '@sentry/node';

export function initSentry(): void {
  const dsn = process.env.SENTRY_DSN;
  if (!dsn) {
    // Sentry disabled in dev if no DSN
    return;
  }

  Sentry.init({
    dsn,
    environment: process.env.NODE_ENV ?? 'development',
    tracesSampleRate: process.env.NODE_ENV === 'production' ? 0.1 : 1.0,
  });
}

export { Sentry };
```

**Commit:**
```bash
git add apps/api/src/lib/sentry.ts
git commit -m "feat(observability): configure Sentry error tracking for API"
```

**PROSSIGA IMEDIATAMENTE para TASK 3.**

---

### TASK 3 de 4 — Criar tRPC request logging middleware

**Arquivos criados:**
- `apps/api/src/trpc/logging-middleware.ts` (novo)

**Código:**

```typescript
// apps/api/src/trpc/logging-middleware.ts
import { createLogger } from '../lib/logger';

const logger = createLogger('trpc');

export interface RequestLogData {
  path: string;
  type: 'query' | 'mutation' | 'subscription';
  tenantId: string | null;
  durationMs: number;
  ok: boolean;
}

export function logRequest(data: RequestLogData): void {
  const level = data.ok ? 'info' : 'error';
  logger[level]({
    path: data.path,
    type: data.type,
    tenantId: data.tenantId,
    durationMs: data.durationMs,
    ok: data.ok,
  }, `${data.type} ${data.path} ${data.durationMs}ms`);
}
```

**Commit:**
```bash
git add apps/api/src/trpc/logging-middleware.ts
git commit -m "feat(observability): add tRPC request logging middleware with tenantId"
```

**PROSSIGA IMEDIATAMENTE para TASK 4.**

---

### TASK 4 de 4 — Inicializar Sentry no startup da API

**Arquivos modificados:**
- `apps/api/src/index.ts` (adicionar Sentry init)

**Código — atualizar apps/api/src/index.ts:**

```typescript
// apps/api/src/index.ts
import { createLogger } from './lib/logger';
import { initSentry } from './lib/sentry';
import { applyTenantMiddleware } from '@wbc/db';
import { getCurrentTenant } from '@wbc/shared';

// Initialize Sentry first
initSentry();

const logger = createLogger('api');

// Apply tenant middleware
applyTenantMiddleware(() => getCurrentTenant()?.tenantId);

logger.info('WBC API starting...');
logger.info('Sentry initialized');
logger.info('Tenant middleware applied');
logger.info('WBC API module loaded successfully');
```

**Commit:**
```bash
git add apps/api/src/index.ts
git commit -m "feat(observability): initialize Sentry on API startup"
```

---

## FINALIZAÇÃO DO ÉPICO

1. Atualizar prompts/STATE.json (epic = COMPLETED, avançar para F1.E06)
2. git add prompts/STATE.json
3. git commit -m "state: F1.E05 complete — advancing to F1.E06"
4. git checkout main
5. git merge fase-01/F1.E05_observability --no-ff
6. git branch -d fase-01/F1.E05_observability
7. git checkout -b fase-01/F1.E06_subscription_entitlements
8. PROSSEGUIR IMEDIATAMENTE para o próximo épico. NÃO PARAR. NÃO PERGUNTAR.

---

## CRITÉRIOS DE CONCLUSÃO (DoD)

- [ ] Logs estruturados em todos os apps
- [ ] Sentry captura erros não tratados
- [ ] Request logging com tenantId, method, duration
- [ ] Zero PII nos logs
