# [F1.E06] — Subscription e Entitlements

> **Fase:** 1 — Fundação
> **Épico:** F1.E06
> **Prioridade:** 6 de 7 (nesta fase)
> **Depende de:** F1.E01 (auth), F1.E02 (tenant), F1.E03 (Redis)
> **Estimativa de tasks:** 4 tasks

> **⚠️ ANTES DE EXECUTAR:** Leia WBC_REGRAS_INVIOLAVEIS.md. As seguintes regras são absolutas:
> - NUNCA pare, NUNCA pergunte, NUNCA espere confirmação
> - Execução sequencial — sem subagents
> - Literalidade absoluta — se não está no prompt, não existe
> - domain/ NUNCA importa de adapters/
> - tenantId OBRIGATÓRIO em toda query Prisma
> - ZERO strings hardcoded na UI — tudo via i18n
> - ZERO `any` — tipagem estrita
> - Commits: Conventional Commits em inglês
> - Sem testes durante construção
> - Ao concluir este épico → merge → próximo épico → NÃO PARAR

---

## CONTEXTO

Gestão de planos Essential/Pro e controle de features por entitlements. A única feature controlada por plano é WhatsApp N2 (API Meta) — exclusivo Pro. Todo o resto é desbloqueado. Cache de entitlements no Redis.

## RESULTADO ESPERADO

- Subscription criada automaticamente ao registrar tenant
- Guard `requirePlan('PRO')` bloqueia features Pro para Essential
- Cache de entitlements funcional (TTL 300s)
- Mudança de plano invalida cache

---

## TASKS

### TASK 1 de 4 — Criar domain e ports de subscription

**Arquivos criados:**
- `packages/business/auth/domain/subscription.ts` (novo)
- `packages/business/auth/ports/subscription-repository.ts` (novo)

**Código:**

```typescript
// packages/business/auth/domain/subscription.ts
import type { Plan } from '@wbc/shared';

export interface Subscription {
  id: string;
  tenantId: string;
  plan: Plan;
  status: string;
  startsAt: Date;
  expiresAt: Date | null;
  aiGenerationsUsed: number;
  aiGenerationsLimit: number;
}

export type Feature = 'WHATSAPP_N2';

const PRO_FEATURES: Feature[] = ['WHATSAPP_N2'];

export function canUseFeature(plan: Plan, feature: Feature): boolean {
  if (PRO_FEATURES.includes(feature)) {
    return plan === 'PRO';
  }
  return true; // All other features available to all plans
}

export function isSubscriptionActive(subscription: Subscription): boolean {
  if (subscription.status !== 'ACTIVE' && subscription.status !== 'TRIAL') {
    return false;
  }
  if (subscription.expiresAt && new Date() > subscription.expiresAt) {
    return false;
  }
  return true;
}

export function hasAIGenerationsRemaining(subscription: Subscription): boolean {
  return subscription.aiGenerationsUsed < subscription.aiGenerationsLimit;
}
```

```typescript
// packages/business/auth/ports/subscription-repository.ts
import type { Subscription } from '../domain/subscription';

export interface SubscriptionRepository {
  findByTenantId(tenantId: string): Promise<Subscription | null>;
  updatePlan(tenantId: string, plan: string): Promise<Subscription>;
  incrementAIUsage(tenantId: string): Promise<number>;
  resetAIUsage(tenantId: string): Promise<void>;
}
```

**Commit:**
```bash
git add packages/business/auth/domain/subscription.ts packages/business/auth/ports/subscription-repository.ts
git commit -m "feat(auth): add subscription domain logic and entitlements"
```

**PROSSIGA IMEDIATAMENTE para TASK 2.**

---

### TASK 2 de 4 — Criar Prisma adapter para subscription

**Arquivos criados:**
- `packages/business/auth/adapters/prisma-subscription-repository.ts` (novo)

**Código:**

```typescript
// packages/business/auth/adapters/prisma-subscription-repository.ts
import { prisma } from '@wbc/db';
import type { SubscriptionRepository } from '../ports/subscription-repository';
import type { Subscription } from '../domain/subscription';
import type { Plan } from '@wbc/shared';

export class PrismaSubscriptionRepository implements SubscriptionRepository {
  async findByTenantId(tenantId: string): Promise<Subscription | null> {
    const sub = await prisma.subscription.findUnique({
      where: { tenantId },
    });
    if (!sub) return null;
    return {
      id: sub.id,
      tenantId: sub.tenantId,
      plan: sub.plan as Plan,
      status: sub.status,
      startsAt: sub.startsAt,
      expiresAt: sub.expiresAt,
      aiGenerationsUsed: sub.aiGenerationsUsed,
      aiGenerationsLimit: sub.aiGenerationsLimit,
    };
  }

  async updatePlan(tenantId: string, plan: string): Promise<Subscription> {
    const sub = await prisma.subscription.update({
      where: { tenantId },
      data: { plan: plan as 'ESSENTIAL' | 'PRO' },
    });
    return {
      id: sub.id,
      tenantId: sub.tenantId,
      plan: sub.plan as Plan,
      status: sub.status,
      startsAt: sub.startsAt,
      expiresAt: sub.expiresAt,
      aiGenerationsUsed: sub.aiGenerationsUsed,
      aiGenerationsLimit: sub.aiGenerationsLimit,
    };
  }

  async incrementAIUsage(tenantId: string): Promise<number> {
    const sub = await prisma.subscription.update({
      where: { tenantId },
      data: { aiGenerationsUsed: { increment: 1 } },
      select: { aiGenerationsUsed: true },
    });
    return sub.aiGenerationsUsed;
  }

  async resetAIUsage(tenantId: string): Promise<void> {
    await prisma.subscription.update({
      where: { tenantId },
      data: { aiGenerationsUsed: 0 },
    });
  }
}
```

**Commit:**
```bash
git add packages/business/auth/adapters/prisma-subscription-repository.ts
git commit -m "feat(auth): add Prisma subscription repository adapter"
```

**PROSSIGA IMEDIATAMENTE para TASK 3.**

---

### TASK 3 de 4 — Criar entitlements cache e guards

**Arquivos criados:**
- `apps/api/src/lib/entitlements.ts` (novo)

**Código:**

```typescript
// apps/api/src/lib/entitlements.ts
import { TRPCError } from '@trpc/server';
import { cacheGet, cacheSet, cacheDelete, CACHE_TTL } from './cache';
import { PrismaSubscriptionRepository } from '../../../../packages/business/auth/adapters/prisma-subscription-repository';
import { canUseFeature, isSubscriptionActive } from '../../../../packages/business/auth/domain/subscription';
import type { Feature } from '../../../../packages/business/auth/domain/subscription';
import type { Plan } from '@wbc/shared';

const subscriptionRepo = new PrismaSubscriptionRepository();

interface CachedEntitlements {
  plan: Plan;
  isActive: boolean;
  aiUsed: number;
  aiLimit: number;
}

function cacheKey(tenantId: string): string {
  return `entitlements:${tenantId}`;
}

export async function getEntitlements(tenantId: string): Promise<CachedEntitlements> {
  // Check cache first
  const cached = await cacheGet<CachedEntitlements>(cacheKey(tenantId));
  if (cached) return cached;

  // Load from DB
  const sub = await subscriptionRepo.findByTenantId(tenantId);
  if (!sub) {
    throw new TRPCError({ code: 'NOT_FOUND', message: 'Subscription not found' });
  }

  const entitlements: CachedEntitlements = {
    plan: sub.plan,
    isActive: isSubscriptionActive(sub),
    aiUsed: sub.aiGenerationsUsed,
    aiLimit: sub.aiGenerationsLimit,
  };

  // Cache for 5 minutes
  await cacheSet(cacheKey(tenantId), entitlements, CACHE_TTL.ENTITLEMENTS);

  return entitlements;
}

export async function invalidateEntitlements(tenantId: string): Promise<void> {
  await cacheDelete(cacheKey(tenantId));
}

export async function requirePlan(tenantId: string, requiredPlan: Plan): Promise<void> {
  const entitlements = await getEntitlements(tenantId);

  if (!entitlements.isActive) {
    throw new TRPCError({ code: 'FORBIDDEN', message: 'Subscription is not active' });
  }

  if (requiredPlan === 'PRO' && entitlements.plan !== 'PRO') {
    throw new TRPCError({ code: 'FORBIDDEN', message: 'This feature requires the Pro plan' });
  }
}

export async function requireFeature(tenantId: string, feature: Feature): Promise<void> {
  const entitlements = await getEntitlements(tenantId);

  if (!entitlements.isActive) {
    throw new TRPCError({ code: 'FORBIDDEN', message: 'Subscription is not active' });
  }

  if (!canUseFeature(entitlements.plan, feature)) {
    throw new TRPCError({ code: 'FORBIDDEN', message: `Feature ${feature} requires Pro plan` });
  }
}
```

**Commit:**
```bash
git add apps/api/src/lib/entitlements.ts
git commit -m "feat(auth): add entitlements cache with Redis and plan guards"
```

**PROSSIGA IMEDIATAMENTE para TASK 4.**

---

### TASK 4 de 4 — Adicionar subscription procedures ao auth router

**Arquivos modificados:**
- `apps/api/src/routers/auth.ts` (adicionar getSubscription)

**Código — adicionar ao authRouter:**

Adicionar ao final do objeto authRouter em `apps/api/src/routers/auth.ts`:

```typescript
  getSubscription: protectedProcedure.query(async ({ ctx }) => {
    const sub = await subscriptionRepo.findByTenantId(ctx.tenant.tenantId);
    if (!sub) {
      throw new TRPCError({ code: 'NOT_FOUND', message: 'Subscription not found' });
    }
    return sub;
  }),
```

Adicionar os imports necessários no topo do arquivo:

```typescript
import { protectedProcedure } from '../trpc/trpc';
import { TRPCError } from '@trpc/server';
import { PrismaSubscriptionRepository } from '../../../../packages/business/auth/adapters/prisma-subscription-repository';

const subscriptionRepo = new PrismaSubscriptionRepository();
```

**Commit:**
```bash
git add apps/api/src/routers/auth.ts
git commit -m "feat(auth): add getSubscription procedure to auth router"
```

---

## FINALIZAÇÃO DO ÉPICO

1. Atualizar prompts/STATE.json (epic = COMPLETED, avançar para F1.E07)
2. git add prompts/STATE.json
3. git commit -m "state: F1.E06 complete — advancing to F1.E07"
4. git checkout main
5. git merge fase-01/F1.E06_subscription_entitlements --no-ff
6. git branch -d fase-01/F1.E06_subscription_entitlements
7. git checkout -b fase-01/F1.E07_email_base
8. PROSSEGUIR IMEDIATAMENTE para o próximo épico. NÃO PARAR. NÃO PERGUNTAR.

---

## CRITÉRIOS DE CONCLUSÃO (DoD)

- [ ] Subscription criada automaticamente ao registrar tenant
- [ ] Guard `requirePlan` bloqueia acesso a features Pro para plano Essential
- [ ] Cache de entitlements funcional
- [ ] Mudança de plano invalida cache
