# [F10.E01] — Auth 2.0: Schema Prisma + Migration

> **Fase:** 10 — Auth 2.0 (Sistema de Login Completo)
> **Epico:** F10.E01
> **Prioridade:** 1 de 5 (nesta fase)
> **Depende de:** Fase 9 completa (v1.3.0-fase-09)
> **Estimativa de tasks:** 6 tasks

> **ANTES DE EXECUTAR:** Leia WBC_REGRAS_INVIOLAVEIS.md. As seguintes regras sao absolutas:
> - NUNCA pare, NUNCA pergunte, NUNCA espere confirmacao
> - Execucao sequencial — sem subagents
> - Literalidade absoluta — se nao esta no prompt, nao existe
> - domain/ NUNCA importa de adapters/
> - tenantId OBRIGATORIO em toda query Prisma
> - ZERO strings hardcoded na UI — tudo via i18n
> - ZERO `any` — tipagem estrita
> - Commits: Conventional Commits em ingles
> - Sem testes durante construcao
> - Ao concluir este epico -> merge -> proximo epico -> NAO PARAR

---

## CONTEXTO

O WBC usa Prisma como ORM com PostgreSQL. O schema atual tem o model Tenant acumulando dados de identidade pessoal (name, phone, email, avatar, role) e dados de negocio. O redesign de auth separa identidade (Account) de workspace (Tenant) com uma tabela de vinculo (TenantMember).

Este epico cria os novos models no Prisma schema: Account, OAuthAccount, TenantMember, Session, Invite. Tambem refatora o model Tenant existente removendo campos de identidade pessoal e adiciona enums necessarios.

O schema Prisma fica em `packages/db/prisma/schema.prisma`.

## PRE-CONDICOES

- [x] Fase 9 completa (v1.3.0-fase-09)
- [x] packages/db com Prisma schema existente
- [x] PostgreSQL rodando via Docker Compose

## RESULTADO ESPERADO

Ao final deste prompt:
- Models Account, OAuthAccount, TenantMember, Session, Invite criados no schema Prisma
- Model Tenant refatorado (campos de identidade removidos)
- Model OtpCode atualizado (accountId em vez de phone)
- Enum InviteStatus e OtpPurpose criados
- Migration aplicada com sucesso
- Seed atualizado com dados dos novos models
- `npx prisma validate` e `npx tsc --noEmit` passam

---

## TASKS

### TASK 1 de 6 — Criar novos models no schema Prisma

**Objetivo:** Adicionar os models Account, OAuthAccount, TenantMember, Session e Invite ao schema Prisma.

**Arquivos modificados:**
- `packages/db/prisma/schema.prisma` (adicionar models)

**Codigo:**

Adicionar ao `packages/db/prisma/schema.prisma` os seguintes models:

```prisma
// ==================== AUTH 2.0 MODELS ====================

model Account {
  id            String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  email         String    @unique
  emailVerified DateTime?
  passwordHash  String?
  name          String
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt

  oauthAccounts  OAuthAccount[]
  tenantMembers  TenantMember[]
  sessions       Session[]
  otpCodes       OtpCode[]

  @@map("accounts")
}

model OAuthAccount {
  id                String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  accountId         String   @db.Uuid
  provider          String
  providerAccountId String
  accessToken       String?
  refreshToken      String?
  expiresAt         Int?
  tokenType         String?
  scope             String?
  idToken           String?
  createdAt         DateTime @default(now())

  account Account @relation(fields: [accountId], references: [id], onDelete: Cascade)

  @@unique([provider, providerAccountId])
  @@index([accountId])
  @@map("oauth_accounts")
}

model TenantMember {
  id          String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  accountId   String    @db.Uuid
  tenantId    String    @db.Uuid
  role        Role      @default(CONSULTANT)
  phone       String?
  displayName String?
  avatar      String?
  isActive    Boolean   @default(true)
  deletedAt   DateTime?
  joinedAt    DateTime  @default(now())
  updatedAt   DateTime  @updatedAt

  account Account @relation(fields: [accountId], references: [id], onDelete: Cascade)
  tenant  Tenant  @relation(fields: [tenantId], references: [id], onDelete: Cascade)

  @@unique([accountId, tenantId])
  @@index([tenantId])
  @@index([accountId])
  @@map("tenant_members")
}

model Session {
  id          String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  accountId   String   @db.Uuid
  tenantId    String?  @db.Uuid
  tokenHash   String   @unique
  expiresAt   DateTime
  userAgent   String?
  ipAddress   String?
  lastUsedAt  DateTime @default(now())
  createdAt   DateTime @default(now())

  account Account @relation(fields: [accountId], references: [id], onDelete: Cascade)

  @@index([accountId])
  @@index([expiresAt])
  @@map("sessions")
}

model Invite {
  id          String       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId    String       @db.Uuid
  email       String
  role        Role         @default(CONSULTANT)
  invitedBy   String       @db.Uuid
  token       String       @unique
  status      InviteStatus @default(PENDING)
  expiresAt   DateTime
  acceptedAt  DateTime?
  createdAt   DateTime     @default(now())

  tenant Tenant @relation(fields: [tenantId], references: [id], onDelete: Cascade)

  @@index([email])
  @@index([tenantId])
  @@map("invites")
}

enum InviteStatus {
  PENDING
  ACCEPTED
  EXPIRED
  CANCELLED
}

enum OtpPurpose {
  TWO_FACTOR
  EMAIL_VERIFICATION
}
```

**Commit:**
```bash
git add packages/db/prisma/schema.prisma
git commit -m "feat(auth): add Account, OAuthAccount, TenantMember, Session, Invite models"
```

**PROSSIGA IMEDIATAMENTE para TASK 2.**

---

### TASK 2 de 6 — Refatorar model Tenant existente

**Objetivo:** Remover campos de identidade pessoal do Tenant e adicionar relations para os novos models. O campo `name` do Tenant permanece mas agora representa o nome do negocio (ex: "Renata Cosmeticos"), nao o nome da pessoa.

**Arquivos modificados:**
- `packages/db/prisma/schema.prisma` (alterar model Tenant)

**Acoes:**

No model Tenant existente:

1. **Remover** os seguintes campos (eles migraram para Account/TenantMember):
   - `phone` (migra para TenantMember)
   - `email` (migra para Account)
   - `avatar` (migra para TenantMember)
   - `role` (migra para TenantMember)
   - `plan` (vive so na Subscription — remover do Tenant se existir)

2. **Adicionar** as seguintes relations ao Tenant:
   - `members TenantMember[]`
   - `invites Invite[]`

3. **NAO alterar:**
   - O campo `name` do Tenant (permanece, agora = nome do negocio)
   - Nenhuma business relation do Tenant (clients, sales, campaigns, etc.)
   - O model Subscription (permanece como esta)

**Commit:**
```bash
git add packages/db/prisma/schema.prisma
git commit -m "refactor(auth): remove identity fields from Tenant, add TenantMember and Invite relations"
```

**PROSSIGA IMEDIATAMENTE para TASK 3.**

---

### TASK 3 de 6 — Atualizar model OtpCode

**Objetivo:** Adaptar o OtpCode para o novo sistema de auth baseado em Account em vez de phone.

**Arquivos modificados:**
- `packages/db/prisma/schema.prisma` (alterar model OtpCode)

**Acoes:**

No model OtpCode existente:

1. **Trocar** campo `phone` por `accountId String @db.Uuid`
2. **Adicionar** campo `purpose OtpPurpose @default(TWO_FACTOR)`
3. **Adicionar** relation: `account Account @relation(fields: [accountId], references: [id], onDelete: Cascade)`
4. **Atualizar** index de `[phone, code]` para `[accountId, code]`
5. **Adicionar** `@@index([accountId])`

**Confirmar** que o enum Role ja existe e contem: CONSULTANT, LEADER, DIRECTOR, ADMIN.

**Commit:**
```bash
git add packages/db/prisma/schema.prisma
git commit -m "refactor(auth): update OtpCode model to use accountId instead of phone"
```

**PROSSIGA IMEDIATAMENTE para TASK 4.**

---

### TASK 4 de 6 — Gerar e aplicar migration

**Objetivo:** Gerar a migration do Prisma para as alteracoes do schema e aplicar no banco de dados.

**Arquivos criados:**
- `packages/db/prisma/migrations/xxx_auth_v2_schema/migration.sql` (gerado automaticamente)

**Acoes:**

```bash
cd packages/db
npx prisma migrate dev --name auth_v2_schema
npx prisma generate
cd ../..
```

Se houver erros de migration por dados existentes incompativeis:
1. Analise o erro
2. Crie uma migration SQL manual se necessario para mover dados (ex: copiar phone/email/avatar do Tenant para TenantMember/Account)
3. Aplique a migration

**Commit:**
```bash
git add packages/db/prisma/migrations/ packages/db/prisma/schema.prisma
git commit -m "feat(database): apply auth v2 schema migration"
```

**PROSSIGA IMEDIATAMENTE para TASK 5.**

---

### TASK 5 de 6 — Atualizar seed com novos models

**Objetivo:** Atualizar o seed script para popular os novos models com dados de desenvolvimento.

**Arquivos modificados:**
- `packages/db/prisma/seed.ts` (adicionar dados dos novos models)

**Acoes:**

Instalar bcryptjs como dependencia do package db se necessario:
```bash
cd packages/db
pnpm add bcryptjs
pnpm add -D @types/bcryptjs
cd ../..
```

Adicionar ao seed existente (NAO substituir dados existentes — apenas adicionar):

1. **1 Account admin:**
   - email: `admin@weavecode.co.uk`
   - name: `"Robson Admin"`
   - passwordHash: `null` (usa OAuth)
   - emailVerified: `new Date()`

2. **1 Account consultora:**
   - email: `renata@teste.com`
   - name: `"Renata Silva"`
   - passwordHash: hash de `"Teste@123"` com bcrypt cost 12
   - emailVerified: `new Date()`

3. **1 TenantMember** vinculando a consultora ao tenant existente com role ADMIN:
   - accountId: id da Account consultora
   - tenantId: id do Tenant existente no seed
   - role: `ADMIN`
   - phone: `"11999990000"`
   - displayName: `"Renata Silva"`

4. **1 TenantMember** vinculando o admin ao tenant existente com role ADMIN:
   - accountId: id da Account admin
   - tenantId: id do Tenant existente no seed
   - role: `ADMIN`

5. **1 Invite pendente de exemplo:**
   - tenantId: id do Tenant existente
   - email: `"convidada@teste.com"`
   - role: `CONSULTANT`
   - invitedBy: id da Account admin
   - token: string unica gerada com `crypto.randomUUID()`
   - status: `PENDING`
   - expiresAt: `new Date(Date.now() + 7 * 24 * 60 * 60 * 1000)` (7 dias)

**Commit:**
```bash
git add packages/db/prisma/seed.ts packages/db/package.json pnpm-lock.yaml
git commit -m "feat(database): update seed with Account, TenantMember and Invite data"
```

**PROSSIGA IMEDIATAMENTE para TASK 6.**

---

### TASK 6 de 6 — Validacao e ajuste de imports

**Objetivo:** Validar que o schema esta correto, o client Prisma gera sem erros, e o type-check do monorepo passa.

**Acoes:**

```bash
# Validar schema
cd packages/db && npx prisma validate && cd ../..

# Gerar client
cd packages/db && npx prisma generate && cd ../..

# Rodar seed
cd packages/db && npx prisma db seed && cd ../..

# Type-check do monorepo
pnpm type-check
```

Se `pnpm type-check` falhar por imports quebrados (ex: codigo que referenciava `tenant.phone` ou `tenant.email` que foram removidos), corrija os imports e ajuste o codigo para buscar esses dados do TenantMember/Account em vez do Tenant.

**Commit:**
```bash
git add -A
git commit -m "fix(auth): adjust broken imports after Tenant schema refactor"
```

---

## FINALIZACAO DO EPICO

Execute EXATAMENTE esta sequencia:

1. Atualizar prompts/STATE.json (epic status = COMPLETED, avancar current_epic para F10.E02)
2. git add prompts/STATE.json
3. git commit -m "state: F10.E01 complete — advancing to F10.E02"
4. git checkout main
5. git merge fase-10/F10.E01_auth_v2_schema --no-ff
6. git branch -d fase-10/F10.E01_auth_v2_schema
7. git checkout -b fase-10/F10.E02_auth_v2_core
8. PROSSEGUIR IMEDIATAMENTE para o proximo epico. NAO PARAR. NAO PERGUNTAR.

---

## CRITERIOS DE CONCLUSAO (DoD)

- [ ] Models Account, OAuthAccount, TenantMember, Session, Invite criados
- [ ] Enum InviteStatus e OtpPurpose criados
- [ ] Tenant refatorado (campos de identidade removidos)
- [ ] OtpCode atualizado (accountId em vez de phone)
- [ ] Migration aplicada sem erros
- [ ] Seed roda sem erros com dados dos novos models
- [ ] `npx prisma validate` passa
- [ ] `npx prisma generate` gera client sem erros
- [ ] `pnpm type-check` passa (imports ajustados)
