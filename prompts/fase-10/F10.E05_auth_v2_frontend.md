# [F10.E05] — Auth 2.0: Frontend Auth (Telas)

> **Fase:** 10 — Auth 2.0 (Sistema de Login Completo)
> **Epico:** F10.E05
> **Prioridade:** 5 de 5 (nesta fase)
> **Depende de:** F10.E01, F10.E02, F10.E03, F10.E04
> **Estimativa de tasks:** 9 tasks

> **ANTES DE EXECUTAR:** Leia WBC_REGRAS_INVIOLAVEIS.md. As seguintes regras sao absolutas:
> - NUNCA pare, NUNCA pergunte, NUNCA espere confirmacao
> - Execucao sequencial — sem subagents
> - Literalidade absoluta — se nao esta no prompt, nao existe
> - domain/ NUNCA importa de adapters/
> - tenantId OBRIGATORIO em toda query Prisma
> - ZERO strings hardcoded na UI — tudo via i18n
> - ZERO `any` — tipagem estrita
> - Commits: Conventional Commits em ingles
> - Sem testes durante construcao
> - Ao concluir este epico -> merge -> proximo epico -> NAO PARAR

---

## CONTEXTO

Os Epicos 1-4 implementaram todo o backend de auth: schema, core, router tRPC, middleware, guards, RLS. Agora precisamos das telas para o usuario interagir: login, registro, onboarding de tenant, onboarding de user (invite), seletor de workspace, e middleware Next.js.

Stack frontend existente:
- Next.js App Router (ja configurado)
- React + TypeScript
- Tailwind CSS + shadcn/ui (ja configurados)
- tRPC client (ja configurado)
- React Query / TanStack Query (ja configurado)
- next-intl para i18n (ja configurado)

## PRE-CONDICOES

- [x] F10.E01 a F10.E04 completos (backend de auth completo)
- [x] Auth.js configurado com Google + Credentials (F10.E02)
- [x] tRPC router com todas as procedures (F10.E03)
- [x] Middleware e guards funcionais (F10.E04)

## RESULTADO ESPERADO

Ao final deste prompt:
- Tela de login (Google + Credentials)
- Tela de registro (Google + email/senha)
- Tela de onboarding (wizard 3 passos: negocio, contato, finalizar)
- Tela de aceitar invite
- Tela de seletor de workspace
- Tela de reset de senha
- Tela de verificacao de email
- Tela de tenant suspenso
- Componentes: GoogleLoginButton, CredentialsForm, OnboardingWizard, WorkspaceSelector, EmailVerificationBanner
- Middleware Next.js atualizado para redirecionar conforme estado do JWT
- Chaves i18n em pt-BR e en

---

## TASKS

### TASK 1 de 9 — Layout de auth e componentes compartilhados

**Objetivo:** Criar o layout limpo (sem sidebar) para paginas de auth e componentes base.

**Arquivos criados:**
- `apps/web/src/app/(auth)/layout.tsx` (novo)
- `apps/web/src/components/auth/google-login-button.tsx` (novo)
- `apps/web/src/components/auth/credentials-form.tsx` (novo)

**Codigo:**

```typescript
// apps/web/src/app/(auth)/layout.tsx
import type { ReactNode } from 'react';

export default function AuthLayout({ children }: { children: ReactNode }) {
  return (
    <div className="flex min-h-screen items-center justify-center bg-background p-4">
      <div className="w-full max-w-md space-y-6">
        {children}
      </div>
    </div>
  );
}
```

```typescript
// apps/web/src/components/auth/google-login-button.tsx
'use client';

import { signIn } from 'next-auth/react';
import { Button } from '@wbc/ui/components/button';
import { useTranslations } from 'next-intl';

export function GoogleLoginButton() {
  const t = useTranslations('auth');
  return (
    <Button
      variant="outline"
      className="w-full"
      onClick={() => signIn('google')}
    >
      <svg className="mr-2 h-4 w-4" viewBox="0 0 24 24">
        {/* Google icon SVG path */}
        <path d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92a5.06 5.06 0 0 1-2.2 3.32v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.1z" fill="#4285F4" />
        <path d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z" fill="#34A853" />
        <path d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.07H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.93l2.85-2.22.81-.62z" fill="#FBBC05" />
        <path d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.07l3.66 2.84c.87-2.6 3.3-4.53 6.16-4.53z" fill="#EA4335" />
      </svg>
      {t('login.google')}
    </Button>
  );
}
```

```typescript
// apps/web/src/components/auth/credentials-form.tsx
'use client';

import { useState } from 'react';
import { signIn } from 'next-auth/react';
import { useRouter } from 'next/navigation';
import { Button } from '@wbc/ui/components/button';
import { Input } from '@wbc/ui/components/input';
import { Label } from '@wbc/ui/components/label';
import { useTranslations } from 'next-intl';

interface CredentialsFormProps {
  mode: 'login' | 'register';
}

export function CredentialsForm({ mode }: CredentialsFormProps) {
  const t = useTranslations('auth');
  const router = useRouter();
  const [email, setEmail] = useState('');
  const [password, setPassword] = useState('');
  const [name, setName] = useState('');
  const [confirmPassword, setConfirmPassword] = useState('');
  const [error, setError] = useState('');
  const [loading, setLoading] = useState(false);

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    setError('');
    setLoading(true);

    try {
      if (mode === 'register') {
        if (password !== confirmPassword) {
          setError(t('register.passwordMismatch'));
          setLoading(false);
          return;
        }
        // TODO: chamar endpoint de criacao de Account via tRPC
        // Apos criar, fazer signIn automatico
      }

      const result = await signIn('credentials', {
        email,
        password,
        redirect: false,
      });

      if (result?.error) {
        setError(t('login.invalidCredentials'));
      } else {
        router.push('/dashboard');
        router.refresh();
      }
    } catch {
      setError(t('login.genericError'));
    } finally {
      setLoading(false);
    }
  };

  return (
    <form onSubmit={handleSubmit} className="space-y-4">
      {mode === 'register' && (
        <div className="space-y-2">
          <Label htmlFor="name">{t('register.name')}</Label>
          <Input
            id="name"
            type="text"
            value={name}
            onChange={(e) => setName(e.target.value)}
            required
            minLength={2}
          />
        </div>
      )}

      <div className="space-y-2">
        <Label htmlFor="email">{t('login.email')}</Label>
        <Input
          id="email"
          type="email"
          value={email}
          onChange={(e) => setEmail(e.target.value)}
          required
        />
      </div>

      <div className="space-y-2">
        <Label htmlFor="password">{t('login.password')}</Label>
        <Input
          id="password"
          type="password"
          value={password}
          onChange={(e) => setPassword(e.target.value)}
          required
          minLength={8}
        />
      </div>

      {mode === 'register' && (
        <div className="space-y-2">
          <Label htmlFor="confirmPassword">{t('register.confirmPassword')}</Label>
          <Input
            id="confirmPassword"
            type="password"
            value={confirmPassword}
            onChange={(e) => setConfirmPassword(e.target.value)}
            required
            minLength={8}
          />
        </div>
      )}

      {error && <p className="text-sm text-destructive">{error}</p>}

      <Button type="submit" className="w-full" disabled={loading}>
        {loading ? '...' : mode === 'login' ? t('login.submit') : t('register.submit')}
      </Button>
    </form>
  );
}
```

**Commit:**
```bash
git add apps/web/src/app/\(auth\)/layout.tsx apps/web/src/components/auth/
git commit -m "feat(auth): add auth layout and shared components — GoogleLoginButton, CredentialsForm"
```

**PROSSIGA IMEDIATAMENTE para TASK 2.**

---

### TASK 2 de 9 — Tela de Login

**Objetivo:** Criar a pagina de login com Google + Credentials.

**Arquivos criados:**
- `apps/web/src/app/(auth)/login/page.tsx` (novo)

**Codigo:**

```typescript
// apps/web/src/app/(auth)/login/page.tsx
import Link from 'next/link';
import { useTranslations } from 'next-intl';
import { GoogleLoginButton } from '@/components/auth/google-login-button';
import { CredentialsForm } from '@/components/auth/credentials-form';

export default function LoginPage() {
  const t = useTranslations('auth');

  return (
    <>
      {/* Logo WBC */}
      <div className="text-center">
        <h1 className="text-2xl font-bold">{t('login.title')}</h1>
      </div>

      {/* Google login */}
      <GoogleLoginButton />

      {/* Separador */}
      <div className="relative">
        <div className="absolute inset-0 flex items-center">
          <span className="w-full border-t" />
        </div>
        <div className="relative flex justify-center text-xs uppercase">
          <span className="bg-background px-2 text-muted-foreground">{t('login.or')}</span>
        </div>
      </div>

      {/* Formulario email + senha */}
      <CredentialsForm mode="login" />

      {/* Links */}
      <div className="text-center text-sm">
        <Link href="/reset-password" className="text-primary hover:underline">
          {t('login.forgotPassword')}
        </Link>
      </div>
      <div className="text-center text-sm">
        <span className="text-muted-foreground">{t('login.noAccount')}</span>{' '}
        <Link href="/register" className="text-primary hover:underline">
          {t('login.createAccount')}
        </Link>
      </div>
    </>
  );
}
```

**Commit:**
```bash
git add apps/web/src/app/\(auth\)/login/
git commit -m "feat(auth): add login page with Google + credentials"
```

**PROSSIGA IMEDIATAMENTE para TASK 3.**

---

### TASK 3 de 9 — Tela de Registro

**Objetivo:** Criar a pagina de registro com Google + email/senha.

**Arquivos criados:**
- `apps/web/src/app/(auth)/register/page.tsx` (novo)

**Codigo:**

```typescript
// apps/web/src/app/(auth)/register/page.tsx
import Link from 'next/link';
import { useTranslations } from 'next-intl';
import { GoogleLoginButton } from '@/components/auth/google-login-button';
import { CredentialsForm } from '@/components/auth/credentials-form';

export default function RegisterPage() {
  const t = useTranslations('auth');

  return (
    <>
      <div className="text-center">
        <h1 className="text-2xl font-bold">{t('register.title')}</h1>
      </div>

      <GoogleLoginButton />

      <div className="relative">
        <div className="absolute inset-0 flex items-center">
          <span className="w-full border-t" />
        </div>
        <div className="relative flex justify-center text-xs uppercase">
          <span className="bg-background px-2 text-muted-foreground">{t('login.or')}</span>
        </div>
      </div>

      <CredentialsForm mode="register" />

      <div className="text-center text-sm">
        <span className="text-muted-foreground">{t('register.hasAccount')}</span>{' '}
        <Link href="/login" className="text-primary hover:underline">
          {t('register.login')}
        </Link>
      </div>
    </>
  );
}
```

**Commit:**
```bash
git add apps/web/src/app/\(auth\)/register/
git commit -m "feat(auth): add register page with Google + credentials"
```

**PROSSIGA IMEDIATAMENTE para TASK 4.**

---

### TASK 4 de 9 — Tela de Onboarding (wizard 3 passos)

**Objetivo:** Criar a pagina de onboarding de tenant com wizard de 3 passos.

**Arquivos criados:**
- `apps/web/src/app/(auth)/onboarding/page.tsx` (novo)
- `apps/web/src/components/auth/onboarding-wizard.tsx` (novo)

**Codigo:**

O wizard tem 3 passos:

**Step 1 — Seu Negocio:**
- Campo: Nome do negocio (ex: "Renata Cosmeticos")
- Campo: Slug (auto-gerado a partir do nome, editavel) com preview: `renata-cosmeticos.wbc.com.br`
- Validacao: slug unico (verificar via tRPC em tempo real com debounce de 500ms)

**Step 2 — Contato:**
- Campo: WhatsApp (com mascara de telefone BR: (XX) XXXXX-XXXX)
- Campo: Marca principal (select com opcoes: Mary Kay, Natura, Avon, Jequiti, Boticario, Outra)
- Validacao: telefone formato valido

**Step 3 — Finalizar:**
- Campo: Foto de perfil (upload opcional com preview)
- Checkbox: "Li e aceito os Termos de Uso e Politica de Privacidade"
- Botao: "Comecar a usar o WBC"

Ao clicar "Comecar": chama `auth.completeOnboarding` via tRPC -> redirect para `/dashboard`.

Usar componentes shadcn/ui existentes: Button, Input, Label, Select, Checkbox.
O wizard usa estado local (useState) para navegacao entre passos.
Todas as labels via `useTranslations('auth')`.

**Commit:**
```bash
git add apps/web/src/app/\(auth\)/onboarding/ apps/web/src/components/auth/onboarding-wizard.tsx
git commit -m "feat(auth): add onboarding wizard — 3-step tenant creation"
```

**PROSSIGA IMEDIATAMENTE para TASK 5.**

---

### TASK 5 de 9 — Tela de Aceitar Invite

**Objetivo:** Criar a pagina de aceitar convite com mini-onboarding de user.

**Arquivos criados:**
- `apps/web/src/app/(auth)/invite/page.tsx` (novo)

**Codigo:**

A tela recebe `?token=xxx` como query param.

- Cabecalho: "Voce foi convidado(a) para o time de {tenantName}!"
- Se nao tem Account e nao esta logado: mostra opcao de criar (Google ou email/senha) com redirect de volta para esta tela apos auth
- Se ja tem Account e esta logado:
  - Campo: Confirmar nome (pre-preenchido do Account.name)
  - Campo: WhatsApp (mascara BR)
  - Botao: "Entrar no time"
- Ao clicar: chama `auth.acceptInvite` via tRPC -> redirect para `/dashboard`

Tratar erros: convite expirado, convite ja aceito, email nao corresponde.
Todas as labels via i18n.

**Commit:**
```bash
git add apps/web/src/app/\(auth\)/invite/
git commit -m "feat(auth): add invite acceptance page with mini onboarding"
```

**PROSSIGA IMEDIATAMENTE para TASK 6.**

---

### TASK 6 de 9 — Tela de Seletor de Workspace

**Objetivo:** Criar a pagina de selecao de workspace para accounts com multiplos tenants.

**Arquivos criados:**
- `apps/web/src/app/(auth)/select-workspace/page.tsx` (novo)
- `apps/web/src/components/auth/workspace-selector.tsx` (novo)

**Codigo:**

- Cabecalho: "Escolha um workspace"
- Lista de cards com os workspaces da Account (obtidos via `auth.listWorkspaces`):
  - Nome do tenant
  - Role (badge colorido: ADMIN=purple, DIRECTOR=blue, LEADER=green, CONSULTANT=gray)
  - Plano (ESSENTIAL / PRO)
- Ao clicar num card: chama `switchWorkspace()` do utilitario criado no Epico 4 -> redirect para `/dashboard`
- Rodape: "Ou crie um novo workspace" -> link para `/onboarding`

Usar TanStack Query para buscar workspaces.
Todas as labels via i18n.

**Commit:**
```bash
git add apps/web/src/app/\(auth\)/select-workspace/ apps/web/src/components/auth/workspace-selector.tsx
git commit -m "feat(auth): add workspace selector page with role badges"
```

**PROSSIGA IMEDIATAMENTE para TASK 7.**

---

### TASK 7 de 9 — Telas de suporte (reset password, verify email, suspended)

**Objetivo:** Criar as paginas de suporte do fluxo de auth.

**Arquivos criados:**
- `apps/web/src/app/(auth)/reset-password/page.tsx` (novo)
- `apps/web/src/app/(auth)/verify-email/page.tsx` (novo)
- `apps/web/src/app/(auth)/suspended/page.tsx` (novo)
- `apps/web/src/components/auth/email-verification-banner.tsx` (novo)

**Codigo:**

**Reset de Senha (`/reset-password`):**
Dois estados:
- Sem token (solicitar): campo email + botao "Enviar link de recuperacao". Chama `auth.requestPasswordReset`.
- Com token (resetar): campos nova senha + confirmar senha + botao "Salvar nova senha". Chama `auth.resetPassword`.

**Verificacao de Email (`/verify-email?token=xxx`):**
- Ao carregar: chama `auth.verifyEmail` com o token
- Sucesso: "Email verificado!" + redirect para dashboard em 3 segundos
- Erro: "Link invalido ou expirado" + botao "Reenviar email de verificacao"

**Tenant Bloqueado (`/suspended`):**
- Icone de alerta
- Mensagem: "Esta conta esta suspensa. Entre em contato com o suporte: suporte@wbc.com.br"
- Se ADMIN: botao adicional "Exportar meus dados"
- Botao: "Voltar para meus workspaces" -> `/select-workspace`

**Email Verification Banner:**
Banner fixo no topo do dashboard (acima da sidebar):
- Mensagem: "Confirme seu email para usar todas as funcionalidades"
- Botao: "Reenviar email"
- Aparece quando session.emailVerified e null
- Dismissavel por 24 horas (localStorage)

Todas as labels via i18n.

**Commit:**
```bash
git add apps/web/src/app/\(auth\)/reset-password/ apps/web/src/app/\(auth\)/verify-email/ apps/web/src/app/\(auth\)/suspended/ apps/web/src/components/auth/email-verification-banner.tsx
git commit -m "feat(auth): add reset-password, verify-email, suspended pages and verification banner"
```

**PROSSIGA IMEDIATAMENTE para TASK 8.**

---

### TASK 8 de 9 — Middleware Next.js

**Objetivo:** Atualizar o middleware Next.js para redirecionar conforme estado do JWT.

**Arquivos modificados:**
- `apps/web/src/middleware.ts` (substituir/atualizar)

**Codigo:**

```typescript
// apps/web/src/middleware.ts
import { auth } from '@/lib/auth';
import { NextResponse } from 'next/server';

const PUBLIC_ROUTES = ['/login', '/register', '/invite', '/reset-password', '/verify-email'];
const ONBOARDING_ROUTE = '/onboarding';
const WORKSPACE_ROUTE = '/select-workspace';
const SUSPENDED_ROUTE = '/suspended';

export default auth((req) => {
  const { pathname } = req.nextUrl;
  const session = req.auth;

  // 1. Rota publica -> passa
  if (PUBLIC_ROUTES.some((route) => pathname.startsWith(route))) {
    return NextResponse.next();
  }

  // 2. Sem session -> redirect /login
  if (!session?.user) {
    return NextResponse.redirect(new URL('/login', req.url));
  }

  const user = session.user;

  // 3. Session sem tenantId
  if (!user.tenantId) {
    if (user.needsOnboarding && pathname !== ONBOARDING_ROUTE) {
      return NextResponse.redirect(new URL(ONBOARDING_ROUTE, req.url));
    }
    if (user.needsWorkspaceSelection && pathname !== WORKSPACE_ROUTE) {
      return NextResponse.redirect(new URL(WORKSPACE_ROUTE, req.url));
    }
  }

  // 4. Permitir rotas de onboarding e workspace se necessario
  if (pathname === ONBOARDING_ROUTE && !user.needsOnboarding && user.tenantId) {
    return NextResponse.redirect(new URL('/dashboard', req.url));
  }
  if (pathname === WORKSPACE_ROUTE && !user.needsWorkspaceSelection && user.tenantId) {
    return NextResponse.redirect(new URL('/dashboard', req.url));
  }

  // 5. TODO: verificar Tenant.isActive === false -> redirect /suspended

  // 6. Tudo OK -> passa
  return NextResponse.next();
});

export const config = {
  matcher: ['/((?!api|_next/static|_next/image|favicon.ico).*)'],
};
```

**Commit:**
```bash
git add apps/web/src/middleware.ts
git commit -m "feat(auth): update Next.js middleware with auth v2 routing logic"
```

**PROSSIGA IMEDIATAMENTE para TASK 9.**

---

### TASK 9 de 9 — Chaves i18n

**Objetivo:** Criar todas as chaves de traducao para as telas de auth em pt-BR e en.

**Arquivos criados/modificados:**
- `packages/i18n/locales/pt-BR/auth.json` (criar/substituir)
- `packages/i18n/locales/en/auth.json` (criar/substituir)

**Codigo:**

```json
// packages/i18n/locales/pt-BR/auth.json
{
  "login": {
    "title": "Entrar no WBC",
    "google": "Entrar com Google",
    "email": "Email",
    "password": "Senha",
    "submit": "Entrar",
    "forgotPassword": "Esqueci minha senha",
    "noAccount": "Nao tem conta?",
    "createAccount": "Criar conta",
    "or": "ou",
    "invalidCredentials": "Email ou senha incorretos",
    "genericError": "Erro ao fazer login. Tente novamente."
  },
  "register": {
    "title": "Criar conta no WBC",
    "google": "Criar conta com Google",
    "name": "Nome completo",
    "email": "Email",
    "password": "Senha",
    "confirmPassword": "Confirmar senha",
    "submit": "Criar conta",
    "hasAccount": "Ja tem conta?",
    "login": "Fazer login",
    "passwordMismatch": "As senhas nao coincidem"
  },
  "onboarding": {
    "step1": {
      "title": "Seu Negocio",
      "businessName": "Nome do seu negocio",
      "businessNamePlaceholder": "Ex: Renata Cosmeticos",
      "slug": "Endereco do seu site",
      "slugPreview": ".wbc.com.br"
    },
    "step2": {
      "title": "Contato",
      "whatsapp": "WhatsApp",
      "whatsappPlaceholder": "(11) 99999-0000",
      "brand": "Marca principal",
      "brandPlaceholder": "Selecione uma marca"
    },
    "step3": {
      "title": "Finalizar",
      "photo": "Foto de perfil (opcional)",
      "terms": "Li e aceito os Termos de Uso e Politica de Privacidade",
      "submit": "Comecar a usar o WBC"
    },
    "next": "Proximo",
    "back": "Voltar"
  },
  "workspace": {
    "title": "Escolha um workspace",
    "createNew": "Ou crie um novo workspace",
    "plan": "Plano"
  },
  "invite": {
    "title": "Voce foi convidado(a) para o time de {tenantName}!",
    "confirmName": "Confirme seu nome",
    "whatsapp": "WhatsApp",
    "join": "Entrar no time",
    "expired": "Este convite expirou",
    "alreadyAccepted": "Este convite ja foi aceito",
    "emailMismatch": "O email do convite nao corresponde a esta conta"
  },
  "resetPassword": {
    "title": "Recuperar senha",
    "email": "Email",
    "submit": "Enviar link de recuperacao",
    "sent": "Link enviado! Verifique seu email.",
    "newPassword": "Nova senha",
    "confirmPassword": "Confirmar nova senha",
    "save": "Salvar nova senha",
    "success": "Senha atualizada com sucesso!"
  },
  "verifyEmail": {
    "success": "Email verificado!",
    "error": "Link invalido ou expirado",
    "resend": "Reenviar email de verificacao",
    "redirecting": "Redirecionando..."
  },
  "suspended": {
    "title": "Conta suspensa",
    "message": "Esta conta esta suspensa. Entre em contato com o suporte: suporte@wbc.com.br",
    "export": "Exportar meus dados",
    "back": "Voltar para meus workspaces"
  },
  "emailBanner": {
    "message": "Confirme seu email para usar todas as funcionalidades",
    "resend": "Reenviar email"
  }
}
```

```json
// packages/i18n/locales/en/auth.json
{
  "login": {
    "title": "Sign in to WBC",
    "google": "Sign in with Google",
    "email": "Email",
    "password": "Password",
    "submit": "Sign in",
    "forgotPassword": "Forgot password?",
    "noAccount": "Don't have an account?",
    "createAccount": "Create account",
    "or": "or",
    "invalidCredentials": "Invalid email or password",
    "genericError": "Error signing in. Please try again."
  },
  "register": {
    "title": "Create a WBC account",
    "google": "Sign up with Google",
    "name": "Full name",
    "email": "Email",
    "password": "Password",
    "confirmPassword": "Confirm password",
    "submit": "Create account",
    "hasAccount": "Already have an account?",
    "login": "Sign in",
    "passwordMismatch": "Passwords don't match"
  },
  "onboarding": {
    "step1": {
      "title": "Your Business",
      "businessName": "Business name",
      "businessNamePlaceholder": "E.g. Renata Cosmetics",
      "slug": "Your website address",
      "slugPreview": ".wbc.com.br"
    },
    "step2": {
      "title": "Contact",
      "whatsapp": "WhatsApp",
      "whatsappPlaceholder": "(11) 99999-0000",
      "brand": "Main brand",
      "brandPlaceholder": "Select a brand"
    },
    "step3": {
      "title": "Finish",
      "photo": "Profile photo (optional)",
      "terms": "I have read and accept the Terms of Service and Privacy Policy",
      "submit": "Start using WBC"
    },
    "next": "Next",
    "back": "Back"
  },
  "workspace": {
    "title": "Choose a workspace",
    "createNew": "Or create a new workspace",
    "plan": "Plan"
  },
  "invite": {
    "title": "You've been invited to {tenantName}'s team!",
    "confirmName": "Confirm your name",
    "whatsapp": "WhatsApp",
    "join": "Join the team",
    "expired": "This invite has expired",
    "alreadyAccepted": "This invite has already been accepted",
    "emailMismatch": "The invite email doesn't match this account"
  },
  "resetPassword": {
    "title": "Reset password",
    "email": "Email",
    "submit": "Send recovery link",
    "sent": "Link sent! Check your email.",
    "newPassword": "New password",
    "confirmPassword": "Confirm new password",
    "save": "Save new password",
    "success": "Password updated successfully!"
  },
  "verifyEmail": {
    "success": "Email verified!",
    "error": "Invalid or expired link",
    "resend": "Resend verification email",
    "redirecting": "Redirecting..."
  },
  "suspended": {
    "title": "Account suspended",
    "message": "This account has been suspended. Contact support: suporte@wbc.com.br",
    "export": "Export my data",
    "back": "Back to my workspaces"
  },
  "emailBanner": {
    "message": "Confirm your email to use all features",
    "resend": "Resend email"
  }
}
```

**Commit:**
```bash
git add packages/i18n/locales/
git commit -m "feat(i18n): add auth v2 translation keys for pt-BR and en"
```

---

## FINALIZACAO DO EPICO

Execute EXATAMENTE esta sequencia:

1. Atualizar prompts/STATE.json (epic F10.E05 = COMPLETED)
2. git add prompts/STATE.json
3. git commit -m "state: F10.E05 complete — advancing to F10.E06 checkpoint"
4. git checkout main
5. git merge fase-10/F10.E05_auth_v2_frontend --no-ff
6. git branch -d fase-10/F10.E05_auth_v2_frontend
7. NAO criar branch — proximo e CHECKPOINT. Executar procedimento de checkpoint diretamente em main.
8. PROSSEGUIR IMEDIATAMENTE para o checkpoint. NAO PARAR. NAO PERGUNTAR.

---

## CRITERIOS DE CONCLUSAO (DoD)

- [ ] Tela de login renderiza com Google + Credentials
- [ ] Tela de registro renderiza formulario completo
- [ ] Tela de onboarding renderiza wizard de 3 passos
- [ ] Tela de invite renderiza mini-onboarding
- [ ] Tela de select-workspace renderiza lista de workspaces
- [ ] Tela de reset-password com dois estados (solicitar/resetar)
- [ ] Tela de verify-email com tratamento de sucesso/erro
- [ ] Tela de suspended com mensagem e acoes
- [ ] Email Verification Banner no dashboard
- [ ] Middleware Next.js redireciona corretamente em todos os cenarios
- [ ] i18n funciona em PT-BR e EN com todas as chaves
- [ ] ZERO strings hardcoded — tudo via useTranslations()
- [ ] `pnpm type-check` passa
